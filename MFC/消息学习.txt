MFC中有3类消息：一般消息、命令消息（菜单和工具栏按钮产生）、按钮消息（窗口中的按钮产生）
MFC通过宏建立一套消息网，通过宏，在每个类中可以建立一个消息映射表及两个对应的映射表获取函数。
注意：尽管在父类和子类中可能存在相同的消息响应函数，但他们不是虚函数，子类继承或父类父类中的同名函数，MFC通过消息映射表机制达到与虚函数类似的效果而又能避免使用虚函数机制完成消息响应所造成 的占用大量内存的缺陷。
继承自CWnd的类可以通过消息映射表拦截各种类型的消息，而除此之外的文档对象和应用程序对象，以及其它继承自CCmdTarget的类则只能拦截到命令消息。