使用延迟加载的原因：
使用隐式调用动态库的方式，但动态库又不再当前目录下，这时，需要设置延迟加载，在加载前，将要加载库的目录添加到path环境变量中，然后，在实际加载的时候，如果当前目录下找不到要加载的库，就会path指定的目录下寻找

使用freamword框架的话，也是需要设置延迟加载，但不是设置path搜索目录，而是根据依赖关系描述文件，由框架智能去相关目录下加载相关的库，这样在实际加载的时候，因为该依赖库已经在内存中了，就不会重复加载  