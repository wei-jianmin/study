在vs中做测试（禁用优化）：
1. 作用域
父对象构造->子对象构造->子对象析构->父对象析构
先声明的全局变量构造->后声明的全局变量构造->后声明的全局变量析构->先声明的全局变量析构

2.windows动态库中，不调用main，而是调用DllMain，通过项目属性/链接器/高级，可以重设入口点，也可以去掉入口函数

3.局部静态变量的作用期是从所在函数被调用（执行到静态变量定义语句后），才开始的

4.类中的静态变量，当该静态变量在被定义时，才真正起效，全局静态变量的有效期早于main函数