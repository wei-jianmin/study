GetAdaptersInfo
    获取各网卡（适配器）的信息，包括名字，uuid，ip，掩码，dhcp服务器地址等等

CreateFile  
    参：学习笔记\Windows核心编程学习笔记 第五版\第十章 同步设备IO与异步设备IO  
    该函数可以创建或打开文件，这里的文件，不仅是普通文件，包括设备、控制台、串口等等
    通过文件名的前缀影响函数如何解析该文件名（普通文件不支持相对路径）
    命名空间：参：file://文件、路径和命名空间.txt
    #define USERMODEDEVICEDIR R("\\.\Global\")
    #define SYSDEVICEDIR      R("\Device\")
    #define USERDEVICEDIR     R("\DosDevices\Global\")
    #define TAP_WIN_SUFFIX    ".tap"    
        
DeviceIoControl 
    将控制代码直接发送到指定的设备驱动程序，导致相应的设备执行相应的操作        
    BOOL DeviceIoControl(
          [in]                HANDLE       hDevice,   //设备句柄，使用CreteFile获得
          [in]                DWORD        dwIoControlCode,  //控制代码
          [in, optional]      LPVOID       lpInBuffer,
          [in]                DWORD        nInBufferSize,
          [out, optional]     LPVOID       lpOutBuffer,
          [in]                DWORD        nOutBufferSize,
          [out, optional]     LPDWORD      lpBytesReturned,
          [in, out, optional] LPOVERLAPPED lpOverlapped
        );
        dwIoControlCode（tap/tun设备时）
            TAP_WIN_IOCTL_CONFIG_TUN
                设置tap/tun设备的ip和子网掩码
            TAP_WIN_IOCTL_CONFIG_DHCP_MASQ : 给tap/tun配置 DHCP 信息
                DHCP是动态主机配置协议的缩写。它是一种存在于应用层的网络管理协议。
                在DHCP的帮助下，可以动态地给网络上的任何设备或节点分配一个互联网协议IP地址，
                使它们可以使用这个IP进行通信。
                网络管理员的任务是将大量的IP地址手动分配给网络中的所有设备。
                然而，在DHCP中，这个任务是自动化的，是集中管理，而不是手工管理。
                无论是小型本地网络还是大型企业网络都实现了DHCP。
                DHCP的基本目标是为主机分配一个唯一的IP地址。
            TAP_WIN_IOCTL_SET_MEDIA_STATUS：启动tap/tun设备
路由表操作
    GetIpForwardTable
        检索 IPv4 路由表。
        IPHLPAPI_DLL_LINKAGE DWORD GetIpForwardTable(
              [out]     PMIB_IPFORWARDTABLE pIpForwardTable,
              [in, out] PULONG              pdwSize,
              [in]      BOOL                bOrder   //是否对结果排序
            );
    DeleteIpForwardEntry   
        删除本地计算机的 IPv4 路由表中的现有路由
        IPHLPAPI_DLL_LINKAGE DWORD DeleteIpForwardEntry(
              [in] PMIB_IPFORWARDROW pRoute
            );
    CreateIpForwardEntry
        IPHLPAPI_DLL_LINKAGE DWORD CreateIpForwardEntry(
              [in] PMIB_IPFORWARDROW pRoute
            );
        在本地计算机的 IPv4 路由表中创建路由
    路由表操作函数相关的数据结构        
        struct _MIB_IPFORWARDTABLE {
                DWORD dwNumEntries;
                MIB_IPFORWARDROW table[ANY_SIZE];
            } MIB_IPFORWARDTABLE, *PMIB_IPFORWARDTABLE;
        typedef struct _MIB_IPFORWARDROW {
                DWORD dwForwardDest;
                DWORD dwForwardMask;
                DWORD dwForwardPolicy;
                DWORD dwForwardNextHop;
                IF_INDEX dwForwardIfIndex;
                union {
                    DWORD dwForwardType;              // Old field name uses DWORD type.
                    MIB_IPFORWARD_TYPE ForwardType;   // New field name uses enum type.
                };
                union {
                    DWORD dwForwardProto;             // Old field name uses DWORD type.
                    MIB_IPFORWARD_PROTO ForwardProto; // New field name uses enum type.
                };
                DWORD dwForwardAge;
                DWORD dwForwardNextHopAS;
                DWORD dwForwardMetric1;
                DWORD dwForwardMetric2;
                DWORD dwForwardMetric3;
                DWORD dwForwardMetric4;
                DWORD dwForwardMetric5;
            } MIB_IPFORWARDROW, *PMIB_IPFORWARDROW;