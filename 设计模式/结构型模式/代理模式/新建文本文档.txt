代理模式 和 被代理的类 继承相同的接口
使用代理类，和使用原类的方法一样
代理类一般采用聚合的方式，内部包含被代理类的指针。

使用场景
当用户要使用某个类 比较费劲时，如访问远程主机中的对象
当控制一个对象的访问权限，为不同级别的用户提供不同的接口访问能力时

代理有点类似于程序快捷方式，对快捷方式的操作 与直接对应用程序操作 几乎一致


扩展：
当java后台 以接口的形式 提供了不同的http服务
c++调用这些接口
此时可以考虑采用代理模式
这虽然与代理模式的定义（代理模式 和 被代理的类 继承相同的接口）有所区别，
但精神上是一样的