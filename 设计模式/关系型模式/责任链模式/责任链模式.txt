责任链模式就是用来替换swith case的模型。

责任链模式的特点是：
    ● 每个责任类都继承自相同的基类
    ● 基类

和switch的对比：
swith 在一个函数中完成多个判断，形成多种分支。从代码上看，这种判断是纵向的。
责任链模式则是在每个链条节点处判断，如果成功，则链条中断。
swith 结构不符合开闭原则，责任链模式符合开闭原则。

责任链模式的代码核心（控制核心）都在基类上，而各个子类负责处理各种分支条件。

任何用switch case的地方，都可以考虑用责任链模式进行替换。