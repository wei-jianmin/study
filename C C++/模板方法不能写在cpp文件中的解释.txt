c++的模板方法是不能写在cpp中的，只能在头文件中

这是因为，模板类型是未定义的，不确定的，可变的，只有当遇到模板的使用者之后，编译器才知道要把模板类型对应成什么数据类型

所以，如果模板类如果出现在cpp中，则编译器编译该cpp文件时，就不知道把这个模板类型编译成什么类型

而如果在头文件中，则这个头文件是不会主动编译的，只有引用了头文件的某个cpp文件，且该cpp文件中碰到使用模板的地方时，才会把该模板参数具体化。