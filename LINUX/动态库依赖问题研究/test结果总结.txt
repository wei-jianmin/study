研究linux下动态库冲突问题（数科调用了qt,又调用了oes，oes有调用了另一版本的qt）
初步测试结果：
    在显示加载动态库的情形下，
        A.bin -> d.so.1
        A.bin -> b.so
        b.so  -> d.so.2
        d.so.1和d.so.2中有同名的函数和全局变量，但是函数输出内容和全局变量值不同，
        测试结果，A 同时正确调到了 d.so.1 和 d.so.2 中的函数和变量。
        一定范围内证明了在这种调用模式下，同时加载同一动态库的不同版本，不会出现冲突问题